use flake

# Native dev (just backend, just frontend, scripts/dev.sh) loads .env.dev.
# .env (git-ignored) is reserved for prod (docker-compose substitution).
# Keep zone: dev feels dev, prod feels prod.
dotenv_if_exists ".env.dev"
