## Ideas and Though Todo
- Switch to chi, once it gets complex and we need more complex routing
- Change to postgres later 
- Siwtch to better ui later 
- Better upload interface with fields
  - imagefile
  - camera type (default)
  - camera metadata (default)
  - colorshcheme switch
  - locations
- Authentication later
- Slidebar with instargram life veiw or reels but for images
