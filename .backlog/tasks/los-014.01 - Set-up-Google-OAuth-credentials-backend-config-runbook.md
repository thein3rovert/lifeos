---
id: LOS-014.01
title: Set up Google OAuth credentials + backend config (runbook)
status: To Do
assignee: []
created_date: '2026-08-16 17:17'
updated_date: '2026-08-16 17:30'
labels: []
dependencies: []
references:
  - 'https://developers.google.com/calendar/api/quickstart/go'
  - .env.example
  - server/cmd/server/main.go
  - doc/smartboard-scheduler-settings-plan.md
parent_task_id: LOS-014
priority: medium
type: feature
ordinal: 13000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Guide the user through the Google Cloud Console setup (or do it live via pairing) and wire the OAuth credentials into the Go backend config.

Steps:
1. Create a Google Cloud project (or use the existing one)
2. Enable the Google Calendar API
3. Configure the OAuth consent screen (type: Desktop app or Web app depending on redirect flow)
4. Create OAuth client ID + secret
5. Register the redirect URI that the backend will use (e.g., `http://localhost:6060/api/calendar/oauth/callback`)
6. Enable the Calendar API scopes: `calendar.events` (read/write), `calendar.readonly` doesn't apply since this is bidirectional
7. Store credentials in `.env` and add empty placeholders to `.env.example`
8. Add required Go dependencies: `google.golang.org/api/calendar/v3`, `golang.org/x/oauth2`, `google.golang.org/api/option`
9. Write a setup runbook at `doc/google-calendar-oauth-setup.md` (mirrors `doc/smartboard-scheduler-settings-plan.md` style) so future agents/users can reproduce the setup

**Reference:** https://developers.google.com/calendar/api/quickstart/go is a good starting point for the Go client lib.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Google Cloud project created, Calendar API enabled, OAuth consent screen configured (type: Desktop app)
- [x] #2 OAuth client ID + client secret generated and stored in .env (not committed); documented in .env.example
- [x] #3 GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI env vars wired into the Go server config
- [x] #4 OAuth redirect URI registered in Google Cloud console matches the backend callback route
- [x] #5 Runbook doc created (e.g., doc/google-calendar-oauth-setup.md) walking a future agent/user through the full Google Cloud console setup
- [x] #6 go.mod updated with required Google SDK packages (e.g., google.golang.org/api/calendar/v3, golang.org/x/oauth2)
<!-- AC:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: thein3rovert
created: 2026-08-16 17:28
---
**Progress: Google Cloud setup steps 1-4 complete.** User walked through the console:

1. ✅ Google Cloud project — using existing project
2. ✅ Calendar API enabled
3. ✅ OAuth consent screen configured (User data, scope: calendar.events read/write)
4. ✅ OAuth client ID created
   - Application type: Web application
   - Name: Lifeos Web client
   - Authorized redirect URI: `http://localhost:6060/api/calendar/oauth/callback`
   - Client ID + Client Secret generated (user has them, not pasted here for security)

**Remaining:**
- Wire `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI` into `.env` (user holds values) and `.env.example` (placeholders)
- Write the runbook at `doc/google-calendar-oauth-setup.md`
- Add Google SDK Go deps to `go.mod`

ACs #1-4 verified done by user in the console. ACs #5 (runbook) + #6 (go.mod) still pending.
---

author: thein3rovert
created: 2026-08-16 17:30
---
**Committed LOS-014.01 work (commit 8312c3a).** All 6 ACs now verifiable:

- ✅ #1 Google Cloud project + Calendar API + consent screen (user did in console)
- ✅ #2 Client ID + Secret generated; Client ID wired into `.env`; Client Secret left as `GOOGLE_CLIENT_SECRET=` placeholder for user to fill in (not pasted in chat for security); `.env.example` documents all placeholders (empty, committed)
- ✅ #3 `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI` added to `server/internal/config/config.go` `Config` struct + `Load()` reads
- ✅ #4 Redirect URI `http://localhost:6060/api/calendar/oauth/callback` registered in Google Cloud console + mirrored in `.env`
- ✅ #5 Runbook written at `doc/google-calendar-oauth-setup.md` (full Google Cloud Console walkthrough + .env wiring + rotation + troubleshooting + production host notes)
- ✅ #6 go.mod updated via `go get google.golang.org/api/calendar/v3 google.golang.org/api/option golang.org/x/oauth2` — added `google.golang.org/api v0.293.0`, `golang.org/x/oauth2 v0.36.0`, plus transitive deps. `go build ./server/...` and `go vet ./server/...` both clean.

**One manual step for @thein3rovert to close out 014.01:**
Open `.env` and fill in the line `GOOGLE_CLIENT_SECRET=` with the secret from the Google Cloud console (it was shown once when you created the client). Save and rebuild the backend — if `go build ./server/...` succeeds the task is fully Done.

Reply when the secret is in and the backend boots so I can mark this Done.
---
<!-- COMMENTS:END -->
